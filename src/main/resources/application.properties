server.port=8081
# short window: allows small burst (capacity tokens) over windowSeconds
ratelimit.short.capacity=5
ratelimit.short.windowSeconds=5

# minute window: long-term rate (capacity tokens per windowSeconds)
ratelimit.minute.capacity=10
ratelimit.minute.windowSeconds=60
